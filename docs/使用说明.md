一.总llm提示词查看方法：
   1.默认关闭，需要启动前开环境变量：
$env:LLM_TRACE="1"
python run.py
如果你还想在控制台同步看到（会很多）：
$env:LLM_TRACE="1"
$env:LLM_TRACE_STDOUT="1"
python run.py
    2.可使用工具读取：
  命令：  python tools/inspect_llm_trace.py --last 1  #1可换成n次
